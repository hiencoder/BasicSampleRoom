package com.example.hiennv.basicsampleroom.callback;

import com.example.hiennv.basicsampleroom.model.ProductEntity;

public interface ProductClickListener {
    void onClick(ProductEntity productEntity);
}
